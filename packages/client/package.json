{
  "name": "@mogul/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=../server/public/client.js --minify --format=iife"
  },
  "dependencies": {
    "@mogul/engine": "*",
    "@mogul/protocol": "*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
